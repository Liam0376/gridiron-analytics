[project]
name = "ffanalytics"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "nflreadpy>=0.1",
    "requests>=2.32",
    "pydantic>=2.9",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
